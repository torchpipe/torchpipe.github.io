---
id: debug
title: 流水线调度系统的调试
type: explainer
---

:::caution
这部分功能有待实现。
:::

在多节点流水线调度中，我们采用了N个线程去负责异步任务的调度。在N==1时，调度系统是串行的，在N大于1时，系统也保证了相关联的数据在同一个线程中进行调度。

一次请求在同一个调度线程中被串行调度。如果不涉及图的跳转，这次请求使用了内部同一个“栈”空间保存中间结果和状态。故此，很容易暂停调度系统，实现节点级别，filter级别的断点调试。

